# Grok Cockpit app icon

The production icon is generated artwork at `build/icon.png` (1024×1024). Electron Builder derives the macOS `.icns`.

Prompt:

> A macOS app icon, square, dark charcoal rounded-square background. Centered abstract cockpit mark: a slim warm-amber horizontal slit like a terminal cursor, above a faint zinc panel edge. No text, no letters, no logo, no photo, generous padding, flat modern, soft studio lighting, crisp edges suitable for a 1024 pixel application icon.
